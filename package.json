{
  "name": "gtd-app",
  "version": "1.0.0",
  "description": "GTD — Getting Things Done",
  "main": "electron/main.js",
  "scripts": {
    "start": "electron .",
    "cap:build": "node scripts/prepare-www.js && npx cap sync android",
    "cap:open": "npm run cap:build && npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/app": "^6.0.0",
    "@capacitor/browser": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/preferences": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "electron": "^31.0.0"
  }
}
